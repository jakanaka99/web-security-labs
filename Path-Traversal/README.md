# Path Traversal

Path traversal is a web vulnerability that allows attackers to access files and directories outside the intended directory.

Attackers use sequences like:

../

to move up directories and access sensitive files such as:

/etc/passwd
